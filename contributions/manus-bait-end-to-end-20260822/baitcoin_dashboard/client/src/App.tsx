/* Observatory Noir: shell enxuto para a central de observação BAIT. Mantém foco em dados, estado e legibilidade. */
import Home from "./pages/Home";

export default function App() {
  return <Home />;
}
