Integration policy:
- Content is stored under a dedicated namespace to avoid path collisions.
- Existing files, folders, commits and branches are not deleted or overwritten.
- Private keys, wallet data, .env files and binary key material are excluded.
- Merge into main is intentionally not performed; maintainers review the branch first.
- The source ZIP is preserved outside the Git repository and its SHA-256 is recorded.
