# Integração segura MMN AI-to-AI

Este diretório foi criado como namespace aditivo na branch de integração. O conteúdo existente do repositório não foi sobrescrito, movido ou excluído. O merge permanece sob responsabilidade dos mantenedores.

A fonte foi copiada do projeto local `/home/ubuntu/mmn-ai-to-ai` excluindo somente diretórios gerados ou de ambiente (`node_modules`, `dist`, `.git`, `.manus-logs`, caches) e arquivos de ambiente. A contagem observada e as exclusões estão registradas em `INTEGRATION_METADATA.json`.

O ZIP legado enviado pelo usuário foi preservado fora do repositório em `/home/ubuntu/upload/MMNAI-to-AI.zip` e referenciado por SHA-256 no metadata, mas não foi versionado porque contém material de banco/configuração que exige revisão de segurança independente. Não foram fabricados arquivos para atingir a contagem 295/299.
