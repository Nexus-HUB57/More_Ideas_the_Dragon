# Pacote de integração segura — Orquestração Fullstack MMN

Este diretório foi criado por importação não destrutiva a partir do arquivo `DesenvolvimentodeAppFullstackcomRoadmapnoGitHub.zip`.

O conteúdo original do repositório foi preservado. Os arquivos da tarefa foram mantidos no subdiretório `source/extracted/`, e o ZIP original foi preservado em `source/`. Nenhum arquivo existente foi substituído ou removido.

Os manifests `SOURCE_MANIFEST.paths.txt`, `SOURCE_MANIFEST.sha256` e `ARCHIVE.sha256` permitem auditoria end-to-end do conteúdo importado.
