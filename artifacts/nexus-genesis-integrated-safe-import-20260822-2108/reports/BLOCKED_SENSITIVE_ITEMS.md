# Blocked sensitive items

The following items were found in the supplied package and were not published to the public repository:

- `PvKeys.txt`
- `nexus_genesis_key.json`
- Three Firebase service-account private-key JSON files
- `Nexus_Project.zip`, which contains `.env` and `PrivateKey_WIF`
- `FundoNexus.zip`, which contains `PvKeys.txt`
- `NexusGenesis_Integrated.zip`, because it contains the sensitive items above

These items remain available in the original user-provided upload for controlled, private handling.
- `crypto-vault.ts`, because its source contains a private-key PEM marker.
